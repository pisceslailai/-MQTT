# Run daily PostgreSQL backups at 02:15.
# Install with: crontab deploy/cron/flow-monitor-backup.cron.example
15 2 * * * cd /opt/mqtt-flow && BACKUP_DIR=/opt/mqtt-flow/backups RETENTION_DAYS=30 bash scripts/backup_postgres.sh >> /opt/mqtt-flow/backups/backup.log 2>&1
