# Run daily PostgreSQL backups at 02:15.
# Install with: crontab deploy/cron/flow-monitor-backup.cron.example
15 2 * * * cd /opt/flow-monitor && BACKUP_DIR=/opt/flow-monitor/backups RETENTION_DAYS=30 bash scripts/backup_postgres.sh >> /opt/flow-monitor/backups/backup.log 2>&1
