# MQTT 流量计监控系统实施计划

## 1. 项目定位

本项目建设一套只读的流量计远程监控与 15 分钟留痕系统。

系统只负责采集、保存、展示和告警，不负责远程配置现场设备，也不向现场设备下发控制指令。

## 2. 总体架构

现场侧：

- 两台流量计
- Modbus 转 MQTT 网关
- 网关通过公网连接服务器 MQTT Broker

服务器侧：

- EMQX：MQTT Broker
- 后端服务：订阅 MQTT、解析数据、入库、生成 15 分钟汇总、提供 API
- PostgreSQL：保存原始数据、汇总数据、设备状态
- 前端页面：展示实时数据、历史趋势、异常状态
- Nginx：前端入口和 Basic Auth

部署方式：

- 使用 Docker Compose 单机部署
- 正式服务器优先选择阿里云大陆地域
- 服务器配置建议从 `2核4G / 80GB SSD / 3-5M 带宽` 起步

## 3. 先在 WSL 环境测试

正式部署云服务器前，先在本地 WSL 环境跑通最小闭环，确认数据链路、业务规则和页面展示都正常。

### 3.1 WSL 测试目标

WSL 阶段不优先处理公网、安全组、域名、HTTPS、真实网关接入等部署问题，重点验证核心业务链路：

- MQTT 数据能被 Broker 接收
- 后端能订阅两台流量计 topic
- 原始数据能入库
- 15 分钟汇总能按规则生成
- 前端能看到实时值和历史趋势
- 离线、时钟偏差、数据缺口等异常能被识别
- Docker 重启后数据不丢

### 3.2 WSL 环境要求

- Windows 开启 WSL2
- 安装 Ubuntu
- 安装 Docker Desktop
- 开启 Docker Desktop 的 WSL integration
- 在 WSL 中确认以下命令可用：

```bash
docker compose version
```

### 3.3 WSL 测试服务

本地 Docker Compose 先运行以下服务：

- `emqx`
- `postgres`
- `backend`
- `frontend`

测试阶段可暂不启用：

- Nginx Basic Auth
- 企业微信/钉钉 Webhook 通知
- TLS
- 云服务器备份策略

这些内容在云服务器部署阶段补齐。

### 3.4 模拟流量计

WSL 测试阶段先不依赖真实 Modbus 转 MQTT 网关，使用模拟脚本发布两台流量计数据。

MQTT topic：

```text
meters/FM001/reading
meters/FM002/reading
```

标准 payload：

```json
{
  "meter_id": "FM001",
  "device_ts": "2026-05-19T10:15:00+08:00",
  "instant_flow": 12.34,
  "total_flow": 56789.01,
  "unit": "m3/h"
}
```

模拟脚本需要覆盖：

- 正常每分钟上报
- 设备离线
- 断线后补传
- 设备时间偏差超过 2 分钟
- 累计流量增长异常

### 3.5 WSL 验收标准

WSL 测试通过需要满足：

- EMQX 能看到模拟客户端连接
- 两台流量计的原始数据进入 `raw_readings`
- 每 15 分钟生成 `interval_readings`
- 15 分钟增量由累计流量差值计算
- 设备时间正常的数据进入对应时间窗口
- 设备时间与服务器接收时间偏差超过 2 分钟时，数据入库但标记异常，不参与正式 15 分钟结算
- 补传数据按设备时间回填历史窗口
- 不支持补传的断线时段被标记为数据缺口
- 前端能显示实时卡片和历史趋势
- Docker Compose 重启后数据库数据仍保留
- PostgreSQL 备份脚本可以导出 dump 文件

## 4. 数据规则

每台流量计同时保存：

- 瞬时流量
- 累计流量

15 分钟汇总规则：

- 以设备时间作为业务时间和分桶依据
- 服务器接收时间作为审计证据
- 用累计流量差值计算 15 分钟增量
- 瞬时流量用于趋势、最大值、最小值、平均值和异常观察
- 设备时间与服务器接收时间偏差超过 2 分钟时，数据标记异常，不参与正式汇总

数据保留规则：

- 原始分钟级数据保留 12 个月
- 15 分钟汇总数据长期保留

## 5. 网关选型要求

网关选型以配置简单为优先，但必须满足以下底线能力：

- 支持 MQTT 用户名密码
- 支持 NTP 校时
- 支持断线重连
- payload 可读，或能明确配置寄存器映射

优先能力：

- 支持离线缓存补传
- 支持 JSON payload
- 支持 TLS
- 支持配置导出和恢复

如果网关只能上传寄存器原始值，后端需要增加映射配置，将原始值转换为标准数据结构。

## 6. 前端与告警

前端首页展示：

- 两台流量计在线状态
- 当前瞬时流量
- 当前累计流量
- 最近 15 分钟增量
- 今日累计
- 异常状态

历史页展示：

- 按时间范围查询
- 15 分钟曲线
- 15 分钟明细表
- 原始数据追溯入口

告警范围：

- 设备离线
- 设备时间偏差超过 2 分钟
- 15 分钟窗口缺数据
- 流量值明显异常

通知渠道：

- v1 使用企业微信或钉钉 Webhook
- WSL 测试阶段可先只做页面告警

## 7. 正式服务器部署

WSL 测试通过后，再部署到云服务器。

正式部署补齐：

- 阿里云服务器采购
- Docker Compose 生产配置
- Nginx Basic Auth
- MQTT 强用户名密码
- 安全组最小开放
- PostgreSQL 每日本地备份
- 每周异地备份
- 企业微信/钉钉 Webhook

公网访问策略：

- 长期使用公网 IP
- v1 不强制 HTTPS 和 MQTT TLS
- 必须使用强密码、安全组最小开放和定期备份

## 8. 交付物

第一阶段交付：

- WSL 可运行的 Docker Compose 项目
- EMQX、PostgreSQL、后端、前端基础服务
- 两台流量计模拟发布脚本
- 数据库初始化脚本
- 15 分钟汇总任务
- 前端实时和历史页面
- WSL 测试说明

第二阶段交付：

- 云服务器部署说明
- 生产 Docker Compose 配置
- Nginx Basic Auth 配置
- 备份脚本
- 企业微信/钉钉通知配置
- 网关接入说明
